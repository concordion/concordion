package org.concordion.api;

/**
 * Defines the scope of fields.
 * 
 * @since 2.0.0
 */
public enum Scope {
    /**
     * Per example (where the example uses the concordion example command).
     */
    EXAMPLE,
    /**
     * Per specification.
     */
    SPECIFICATION
}
